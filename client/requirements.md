## Packages
framer-motion | Complex animations (parallax, fade-ins, scroll-triggered)
react-scroll | Smooth scrolling for navigation links
lucide-react | Icon library (already in base, but emphasizing usage)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
- Images are located at `/images/profile-formal.jpeg` and `/images/profile-casual.jpeg`.
- The backend provides `POST /api/contact` for the contact form.
- Google Fonts (Outfit and DM Sans) will be imported in index.css.
